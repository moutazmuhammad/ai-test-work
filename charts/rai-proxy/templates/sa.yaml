apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  {{- if .Values.sa_annotations }}
  annotations:
    {{- toYaml .Values.sa_annotations | nindent 4 }}
  {{- end }}